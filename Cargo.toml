[package]
name = "probfilter"
version = "0.1.0"
edition = "2024"

[dependencies]
siphasher = "1.0.2"

[dev-dependencies]
rand = "0.9"
criterion = { version = "0.5", features = ["html_reports"] }

[[bench]]
name = "bloom"
harness = false
